# Strategic Brief

> 매주 일요일 밤 한 번씩 다시 읽어라.
> 결정에 막히면 이 문서로 돌아와라.
> 이 문서가 흔들리면 회사가 흔들린다.

---

## 0. 단 한 줄

> **"우리는 가성불량 제거기를 만드는 게 아니라, SMT 검사 데이터의 표준 layer를 만든다. 2차 검사기는 그 첫 wedge일 뿐이다."**

이 한 줄이 무너지면 다 무너진다.

---

## 1. 세 개의 렌즈 — 매 결정마다 동시에 봐라

### 1.1 빌 게이츠 렌즈 — 플랫폼 > 제품
> *"다른 사람이 우리 위에서 빌드해줄 수 있나?"*

- 표준을 정의한 자가 권력을 갖는다 (Bluetooth는 노키아 것이 아니다)
- 어댑터 SDK를 외부에 공개하지 않으면 그건 SaaS이지 플랫폼이 아니다
- 12-24개월 안에 vendor-side 엔지니어가 자기 어댑터 직접 짜는 그림이 나와야 함
- 개발자 문서 = 영업 문서 (Stripe·Twilio가 보여준 길)

### 1.2 워렌 버핏 렌즈 — 해자 > 시장
> *"18개월 뒤에도 우리만 갖고 있는 것은 무엇인가?"*

- 모델은 카피된다. **데이터·스키마·고객 신뢰는 카피 안 된다**
- 가격은 비용이 아니라 ROI에서 나온다 — ₩5M = "무시해도 됨"의 시그널
- Switching cost는 시간에 비례해 커지도록 설계해야 함 (§5 별도 섹션)
- 이해 못하는 사업에 시간 쓰지 말 것 — 우리가 이해하는 것: SMT 검사 워크플로우

### 1.3 일론 머스크 렌즈 — First Principle > 관습
> *"왜 가성불량이 30%인가?"*

- AOI 벤더가 sensitivity > specificity로 튜닝하니까 (escape 1건 책임 vs FC 100건 무시)
- → 우리는 그 비대칭을 흡수하는 **인프라 레이어** = 벤더의 적이 아니라 **보완재**
- 90일 production 1건 > 365일 perfect 솔루션
- 매뉴팩처링 그 자체가 제품이다 — 공장 운영을 더 좋게 만드는 것이 진짜 가치

### 1.4 안드레이 카파시 렌즈 — 데이터 > 아키텍처, Eval > Vibes
> *"네 데이터를 직접 봐라. 200장."*

위 세 렌즈가 사업·전략 차원이라면, **Karpathy 렌즈는 ML 실행 규율**. 이게 없으면 좋은 전략도 잘못된 모델로 무너진다.

- **Look at your data.** 가성불량 200건, 진성불량 200건을 직접 눈으로 보기 전에는 어떤 모델 아키텍처도 결정하지 말 것. 라벨이 5% 잘못되면 Mamba든 무엇이든 의미 없음. 매주 1시간은 **데이터 자체와 시간 보내기**.
- **Eval first, train second.** Training loop보다 eval pipeline이 먼저 돌아가야 함. Cumulative cost curve, escape rate at fixed FC budget, AURC — 이 세 곡선이 hold-out에서 자동 산출되기 전엔 모델 학습 자체가 의미 없음.
- **Data > Architecture, 거의 항상.** 시간 분배는 `data/` 폴더 70%, `models/` 30%. 라벨 품질·다양성·균형 먼저, 아키텍처는 그 다음.
- **The simplest thing that beats the baseline ships.** ConvNeXt-T + class-weighted focal loss + tuned threshold가 v0 베이스라인. **이걸 못 이기는 Mamba RL은 출시 안 함.** 통계적 유의성으로 베이스라인을 이긴 것만 진행.
- **Tight loops > grand plans.** 5분 학습 → 예측 100개 직접 보기 → 데이터 고치기 → 재학습. 50번 반복. 12시간 학습 한 번 돌려놓고 기도하지 말 것.
- **Reproduce yourself.** 같은 학습을 seed 3개로 돌려라. 분산이 개선폭보다 크면 그 개선은 가짜다. 모든 실험은 git commit hash + config로 재현 가능해야 함.
- **Build in public, releases compound.** 공개 스펙, 공개 eval 셋(필요하면 합성), 공개 벤치마크 숫자. 이게 신뢰로, 신뢰가 계약으로 누적됨. (게이츠 렌즈와 직결)
- **If you can't explain it simply, the customer can't buy it.** "Lagrangian PPO with hard escape constraint" 대신 "운영자 200시간/월/라인을 절감". ML 용어를 ROI로 번역하는 것은 영업 도구가 아니라 **본인이 이해했는지 확인하는 도구**.

#### 1.4.1 Karpathy의 ML 디버깅 사다리 (막혔을 때 위에서부터)

```
1. 데이터를 봤는가?           ← 90%는 여기서 답이 나온다
2. 라벨이 정확한가?           ← 사람도 헷갈리는 케이스는 모델도 헷갈린다
3. 베이스라인을 잡았는가?     ← 0.5 random vs 우리 모델 비교
4. 데이터·모델·loss 일치하나? ← 의도한 것을 정말 학습 중인가
5. Loss가 떨어지긴 하나?      ← train loss조차 안 떨어지면 버그
6. 일반화가 되나?             ← train-val gap이 정상 범위인가
7. 그 다음에 아키텍처 고민    ← 위 6개 다 통과했을 때만
```

**대부분 사람들은 7번부터 시작한다. 그래서 시간을 잃는다.**

---

## 2. 매일 결정 직전 5초 필터

결정 누르기 전에 머릿속으로 통과시켜라:

```
1. 데이터 플라이휠을 빠르게 하나?           (버핏)
2. 표준화 쪽인가, 커스텀 쪽인가?            (게이츠)
3. 90일 내 production에 넣을 수 있나?       (머스크)
4. 18개월 후에도 우리만 가능한가?           (버핏)
5. ₩100M 회사로 끝나나, ₩10B로 가나?        (셋 다)
6. 베이스라인이 있고, 이게 그걸 이기나?     (Karpathy)
7. 데이터를 직접 봤나? 라벨은 정확한가?    (Karpathy)
```

7개 중 4개 이상 "yes"면 진행. 아니면 멈추고 다시 설계.

**6, 7번은 코드·모델 결정에만, 1-5는 사업·아키텍처 결정에 비중 ↑.**

---

## 3. 안티패턴 — 절대 하지 마라

| 함정 | 왜 위험 |
|------|---------|
| 모델 더 정교화 | 이미 충분하다. 첫 production이 더 급함 |
| LOI 0건에 가격 협상 | leverage 0인 협상은 항상 진다 |
| DGX Spark 자본 지출 | 첫 LOI 받기 전에는 Colab + 기존 GPU로 충분 |
| 코드 99% 완성도 추구 | production 1건이 코드 1만 줄보다 무겁다 |
| 단일 anchor 의존 | 협상 종료 = 회사 종료. 항상 3개 동시 |
| 모든 어댑터 직접 작성 | 외부 개발자가 짜야 플랫폼이 된다 |
| 고객 데이터 클라우드 export | 온프레 약속을 첫날 깨지 말 것 |
| 경쟁사 가격 따라가기 | 우리는 ROI 가격 — 경쟁사는 비용 가격 |
| **데이터 안 보고 모델 결정** | 200건 직접 보기 전 아키텍처 회의는 시간 낭비 (Karpathy) |
| **Eval 없이 학습** | 측정 안 되면 개선 안 된다. 먼저 곡선, 그 다음 모델 |
| **베이스라인 없이 자랑** | "정확도 95%" — 베이스라인이 96%면 우리가 더 나쁘다 |
| **Seed 1개로 비교 결정** | 분산이 개선폭보다 크면 그 개선은 가짜 |
| **ML 용어로 영업** | "Lagrangian PPO" 대신 "월 200시간 절감" |

---

## 4. 자본·시간 배분

시간 100을 어디에 쓰나:

| 항목 | 현재 (추정) | 권장 | 변화 |
|------|------------|------|------|
| 모델 정교화 | 50 | 15 | ↓↓ |
| 어댑터 SDK·표준화 | 20 | 30 | ↑ |
| **영업·고객 확보** | **5** | **35** | **↑↑↑ ⭐** |
| 데이터 플라이휠 | 5 | 15 | ↑ |
| 기타 (HW·UI·문서) | 20 | 5 | ↓ |

**가장 큰 시정**: 영업 5 → 35. 모델은 충분하다. 부족한 것은 첫 logo다.

---

## 5. Switching Cost — 해자의 실체

> **"고객이 우리 솔루션에서 다른 솔루션으로 바꿀 때 드는 모든 고통의 총량."**
> 시간에 비례해 커지도록 의식적으로 설계해야 함.

### 5.1 시간 곡선

```
바꾸는 비용 (= switching cost)

  높음 │                                    ╱── 3년차: "이걸 빼면 라인 멈춤"
       │                              ╱──
       │                         ╱──
       │                    ╱──
       │              ╱──
       │        ╱── 1년차: 큰 결단 필요
       │   ╱──
       │ ╱
  낮음 │╱  1주차: 그냥 박스 빼면 됨
       └────────────────────────────────►
                  사용 기간
```

**좋은 사업은 이 곡선이 가파르게 올라가도록 의식적으로 설계**한다. 평평하면 누가 더 싼 솔루션 내면 다음 분기에 교체된다.

### 5.2 우리 사업의 두 가지 메커니즘

#### 🧠 라벨 누적 (Label Accumulation)

운영자가 매일 PASS/DEFECT 클릭하는 행위 자체가 라벨이다.

| 기간 | 누적 라벨 | 의미 |
|------|----------|------|
| 1주차 | ~5,000건 | 모델이 일반적 베이스라인 그대로 |
| 6개월 | ~150,000건 | 그 라인 특유의 false-call 패턴 학습 |
| 3년차 | ~1,000,000건 | 모델이 그 고객 라인에 완전히 특화 — 다른 누구도 못 만드는 모델 |

**고객이 떠나려고 하면**: 라벨 export는 가능하지만, 그걸 받아서 즉시 우리 수준 모델을 만들 수 있는 회사가 없음 → 가성불량률이 5% → 30%로 회귀 → 운영자 야근, 라인 처리량 ↓, 부서장 KPI 박살. 결론: **"지금 5%인데 30%로 돌아가는 위험 못 감당"** → 떠날 수 없음.

#### 🕸️ 오케스트레이션 깊이 (Orchestration Depth)

처음엔 작게 박혀있다가 점점 더 깊이 침투한다.

```
Phase 1 (0-12개월) : 2차 검사기 옆 어시스턴트
                     떼는 비용: 박스 1개 빼면 끝

Phase 2 (12-24개월): SPI/ICT/functional test 데이터 흡수, 라인 traceability
                     떼는 비용: 다른 시스템도 연동 끊어야 함

Phase 3 (24-36개월): MES augmentation, 수율 KPI 산출 책임
                     떼는 비용: 운영 SW 일부 다시 짜야 함, 라인 정지

Phase 4 (36+)      : 생산 routing 결정 일부 담당
                     떼는 비용: 거의 회사 시스템 갈아엎기
```

단순 가성불량 분류기로만 머물면 Phase 1에서 끝난다. **인접 워크플로우를 한 단계씩 흡수**하도록 설계해야 깊이가 생긴다.

### 5.3 의식적으로 해야 할 설계 결정

| 결정 | Switching cost ↑ 방향 (해야 할 것) | Switching cost ↓ 방향 (피해야 할 것) |
|------|-----------------------------------|--------------------------------------|
| 라벨 데이터 | 우리 SQLite에 누적, export는 가능하나 우리 모델 없이는 의미 약함 | 표준 포맷 즉시 export, 어디서나 그대로 사용 가능 |
| MES 통합 | 우리만의 augmented data가 MES로 흐름 (sidecar) | 단순 verdict pass-through |
| KPI 대시보드 | 운영자·관리자 일과 화면이 **우리 UI** | 단순 이메일 리포트 |
| 어댑터 범위 | 점진적으로 SPI/ICT/functional test까지 흡수 | AOI 한 종류만 |
| 학습 모델 | 그 라인 데이터로 점점 특화되는 사이트 모델 | 모든 고객에 동일한 글로벌 모델 |

오른쪽 컬럼대로 만들면 = 평범한 SaaS. 왼쪽 컬럼대로 만들면 = Salesforce급 lock-in.

### 5.4 비유

- **AWS**: 1주차 = EC2 서버 1대 빌림 (떠나기 쉬움). 5년차 = PB 데이터 + Lambda + DynamoDB + IAM + VPC → 옮기는 데만 1년 + 수십억. 5년 이상 고객 이탈률 1% 미만.
- **Salesforce**: 1주차 = CRM 1라이선스. 5년차 = 영업·마케팅·고객지원·파트너·BI + 영업사원 머슬 메모리 + 50개 커스텀 앱 → 교체 = 영업 6개월 정지.

**우리는 이걸 SMT 검사·제조에서 해야 한다.**

---

## 6. 이번 주 4가지 베팅

```
[1] aoi-common-spec 공개      → 표준 권력 선점
[2] ₩0 무료 파일럿 프로그램   → anchor 락인
[3] 의사결정자 3명 미팅       → LOI 시드
[4] DGX Spark 주문 보류       → 자본 보존
```

이 순서대로. (1)+(2)는 코드/문서 작업. (3)+(4)는 사업 결정.

---

## 7. 10년 후 — 두 갈래 길

| | A. ₩100M 회사 | B. ₩10B+ 회사 |
|---|---|---|
| 모습 | Korean Tier-1 5-10곳 솔루션 | 글로벌 SMT 검사 데이터 표준 layer |
| 결말 | incumbent에 인수 | Tesla가 자동차에 한 일을 SMT 인프라에 함 |
| 분기점 | 스키마 비공개, 단일 고객 라이선스 | **스키마 공개, 데이터 플라이휠, 다중 anchor** |
| 매출 모델 | per-line 라이선스 | 데이터 플랫폼 + 앱 마켓 + 표준 사용료 |

**6개월 후엔 이미 갈래가 정해진다.** 지금 결정하는 모든 것이 둘 중 하나로 잠긴다.

---

## 8. 한 문장 만트라

출근길에 한 번씩 읊어라:

> **"코드 1줄보다 anchor 고객 1명. 스펙 공개가 라인 코드보다 무겁다. 데이터 200장이 아키텍처 1개보다 가치 있다. 18개월 후 카피되지 않을 것에만 시간을 쓴다."**

---

## 9. 사용법

이 문서는 한 번 읽고 끝나는 게 아니다.

- **매주 일요일 밤**: 처음부터 끝까지 다시 읽는다 (10분)
- **결정에 막힐 때**: §2 (5초 필터)로 직행
- **새 feature·코드 시작 전**: §3 (안티패턴) 체크
- **분기마다**: §7 (10년 갈래)에서 우리가 어느 쪽으로 가고 있는지 점검
- **6개월마다**: 문서 자체를 업데이트 — 어느 가정이 틀렸는지, 어느 베팅이 결과를 냈는지

머릿속에서 흐려지면 다시 펼쳐라.

---

*Last updated: 2026-04-29*
*Drafted under the lens of Bill Gates / Warren Buffett / Elon Musk / Andrej Karpathy synthesis.*
*— First three for the company. Karpathy for the model.*
